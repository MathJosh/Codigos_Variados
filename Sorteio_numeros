#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main() {
int i, numeroSorteado;


srand(time(NULL));

for(i=1; i<=6; i++) {

numeroSorteado = rand() % 60 +1;
printf("Numero %d: %d\n", i, numeroSorteado);      
}

printf("\n\n\n");
system("pause");
return 0;
}
